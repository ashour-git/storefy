# 7. Add empty + error states to admin panels

**Label:** ready-for-agent, ux

## What to build

Add empty state illustrations and error state UI to all admin panel components (ShippingZonePanel, CategoryPanel, DiscountPanel, SimpleCrudPanel). Currently most panels show nothing when empty or erroring.

## Acceptance criteria

- [ ] Empty state shown when no data (with icon + CTA button)
- [ ] Error state shown on DB/API failure (with retry button)
- [ ] Loading state already exists (skeletons) — no regression

## Blocked by

None - can start immediately
